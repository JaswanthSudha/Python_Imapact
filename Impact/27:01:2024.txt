Ao(baseAdress+sizeof(dt)+IV(index value))Ardress of(Storing)
Co(baseAdress+sizeof(dt)+IV(index value))Content of(accesing)
5000+2+0
#include<stdio>
int main(){
	int a=10;5004->5005
	int b[2]={1,2};5000->5001,5002->5003
	int c=20;4998->4999
	printf("\n %d%d",b[0],b[1]);
	printf("\n %d%d",b[2],b[-1]);
	return 0;
}
b[2]=5000+(2*2)=5004=>10
b[-1]=5000+(2*-1)=4998=>20
op[]=*op1=op[]
&* if this operators are in side by side then this is nullfied no symbol is present
on success malloc will return the base address of the allotted memory
on failure malloc will return null
struct node{
int value;
struct node *ptr;//self referencial pointer
	
}


